import React from "react";
// import "./style.css";

// props: bookName, author, link, img, info, apiHref.
function ListCard(props) {

    // function handleSelect(event) {
    //     event.preventDefault();

    //     // when clicked on set the val to.
    //     });
    // }

    return (
        <li>
            {props.companyName}
        </li>
    );
}

export default ListCard;