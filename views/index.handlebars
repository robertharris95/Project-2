<div id="product list" class="container">
    <ul>  
        {{#each product}}
        <li>{{product.name}}, {{product.QTY}}, {{product.pricing}}, {{product.company}}, <a href="{{product.contract}}">View Contract</a></li>
        {{/each}}
    </ul>
</div>
